Drop project screenshots here, named after the project slug:

  workforge.png   (or .jpg / .webp)
  readora.png
  ridezio.png
  hash.png

Recommended: 1600×1000 (16:10), under ~400 KB.

Anything missing falls back to a generated placeholder automatically —
the site won't show a broken image, so you can add them one at a time.
